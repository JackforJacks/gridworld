// No require path changes needed for scene.js
// Three.js/scene/tile helpers

function createScene(
    // ...existing code...
) {
    // ...existing code...
}

module.exports = {
    createScene,
    tick,
    onWindowResize,
    onMouseWheel,
    onMouseDown,
    onMouseMove,
    onMouseUp,
    checkTileIntersection
};
