// Initialization logic for GridWorld
const { createScene, tick } = require('./scene');
const { terrainColors, isLand, updateDashboard } = require('../utils/utils');

// ...existing code...

module.exports = { initializeAndStartGame };
